---
wave: 4
depends_on: [21-01, 21-03]
files_modified:
  - README.md
  - CHANGELOG.md
  - GETTING_STARTED.md
  - PLUGIN_DOCUMENTATION.md
  - CONTRIBUTING.md
  - FAQ.md
  - src/main/java/io/dyuti/osvplugin/parser/GradleParser.kt
autonomous: true
---

# Plan 21-04: Documentation — README, CHANGELOG, FAQ, Contributing

## Goal

Update all user-facing and developer-facing documentation to accurately reflect the v1.1.0 feature set. Create missing docs (CHANGELOG, CONTRIBUTING, FAQ). Document known parser limitations. Ensure every document is release-ready.

## Tasks

<task>
<id>21-04-a</id>
<title>Update README.md with complete v1.1.0 feature set and competitive matrix</title>
<read_first>
  - README.md
  - .planning/STATE.md (competitive position table)
  - .planning/phases/21-final-polish-marketplace-release/21-RESEARCH.md (section 5.1)
</read_first>
<action>
Rewrite README.md with the following structure:

1. **Header**: Keep title + one-line description.
2. **Badges**: Add build status, marketplace version, downloads (placeholder URLs).
3. **Features** section with all v1.1.0 features organized by category:
   - Core: dependency parsing (Maven, Gradle, npm, pip), OSV API integration, tool window, inline inspection, quick fixes
   - Advanced: vulnerable API detection, malicious package detection, basic SAST/taint analysis, privacy-preserving queries, risk scoring (EPSS + CISA KEV), policy enforcement, team collaboration, differential analysis, historical trending, SBOM generation, configuration audit, IDE notifications
   - Integration: SARIF export, license scanning, GitHub Advisory
4. **Quick Start**: 3-step install and 3-step usage summary.
5. **Screenshots**: Reference 3 screenshots (tool window, inline highlight, quick-fix) — note these are placeholders to be captured in Wave 5.
6. **Feature Comparison Table**: Use the exact table from STATE.md competitive position:
   - Features listed: Free SCA, Problems tab, Auto-fix, License scanning, Reachability, Malicious packages, Basic SAST, Privacy exports, Risk scoring, Policy enforcement, Team config sharing, Differential analysis, Historical trends, Config audit, Tree-based UI
   - Columns: OSV Plugin, Snyk, Mend.io, Qodana
   - Use ✅/❌/Free tier/Paid/Premium markers as in STATE.md
7. **Installation**: Marketplace + from-source instructions.
8. **Requirements**: IntelliJ IDEA 2023.3+, Java 17.
9. **License**: Apache 2.0.

Remove any stale references to "Data Flow Analysis", "Organization Management", "Jira Integration" as headline features — they exist as stubs or behind disabled flags. Only list features that are actually implemented and functional.
</action>
<acceptance_criteria>

- README.md contains a Feature Comparison Table with at least 15 rows and 4 columns (OSV Plugin, Snyk, Mend.io, Qodana)
- README.md lists all Phases 6–20 features under an "Advanced Features" section
- README.md contains no references to unimplemented features as if they are fully functional
- README.md references 3 screenshot placeholders (tool window, inline highlight, quick-fix)
- `./gradlew buildPlugin` is unaffected (docs only)
  </acceptance_criteria>
  </task>

<task>
<id>21-04-b</id>
<title>Create CHANGELOG.md with Keep a Changelog format for v1.0.0 and v1.1.0</title>
<read_first>
  - .planning/ROADMAP.md (phases 1–20 summary)
  - .planning/phases/21-final-polish-marketplace-release/21-RESEARCH.md (section 5.2)
</read_first>
<action>
Create CHANGELOG.md in the project root with this exact content structure:

```markdown
# Changelog

All notable changes to the OSV IntelliJ Plugin are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [1.1.0] - 2024-XX-XX

### Added

- Vulnerable API Detection — cross-references vulnerable function signatures with call sites in source code
- Malicious Package Detection — typosquatting, homoglyph, and known-malware detection
- Basic SAST / Taint Analysis — pattern-based SQL injection, XSS, and path-traversal detection
- Privacy-Preserving Queries — SHA-256 hashing of package names in UI, logs, and exports
- Risk Scoring — composite scoring using EPSS + CISA KEV + CVSS for exploitability-based prioritization
- Policy Enforcement — organization-wide dependency compliance policies with severity/CVSS/KEV/license rules
- Team Collaboration — project-level config sharing via `.idea/osv-plugin-config.json`
- Differential Analysis — compare two scans to detect NEW, RESOLVED, and severity-changed vulnerabilities
- Historical Trending — track vulnerability counts over time with ASCII sparkline display
- SBOM Generation — export CycloneDX 1.5 and SPDX 2.3 in JSON and Tag-Value formats
- Configuration Audit — scan `application.properties`/`application.yml` for 20 insecure patterns
- IDE Notification Service — severity-based balloon notifications for discovered vulnerabilities
- Status Bar Widget — persistent scan status and vulnerability count in the IDE status bar
- Dark Mode Support — all UI colors use JBColor for automatic Darcula/light theme adaptation
- MockWebServer Testing — all API tests use mocked HTTP instead of live OSV API calls

### Changed

- Replaced OkHttp with `java.net.http.HttpClient` for better platform compatibility
- Converted inspections to async with per-file caching and debounce to eliminate UI freezing
- Replaced string-based auto-fix with PSI-based refactoring supporting transitive dependencies

### Fixed

- CacheManager singleton bug — `getInstance()` now returns a true singleton via `ApplicationManager.getService()`
- OsVApiService singleton bug — same fix, restoring correct rate limiting behavior
- Hardcoded OSV API URL — now configurable in Settings → OSV Scanner
- Exception swallowing — all `System.err.println` replaced with `com.intellij.openapi.diagnostic.Logger`
- Rate limiting reset on every call — fixed by proper singleton lifecycle
- Vulnerability `packageName` field — now populated from query context instead of empty string

## [1.0.0] - 2024-04-24

### Added

- Core dependency parsing for Maven (pom.xml), Gradle (build.gradle), npm (package-lock.json), pip (requirements.txt)
- OSV API integration with batch queries and caching
- Tool window with vulnerability tree, filtering, and sorting
- Multi-language local inspections (XML, Groovy, Kotlin, JSON, plain text)
- Alt+Enter quick fixes for upgrading vulnerable dependencies
- SARIF export for CI/CD integration
- Settings configuration panel (severity threshold, cache TTL, scan scope)
- License scanning with SPDX identifier support
```

Replace `202X-XX-XX` with `2026-05-12` (the current date).
</action>
<acceptance_criteria>

- CHANGELOG.md exists in project root
- CHANGELOG.md uses Keep a Changelog format with `## [1.1.0]` and `## [1.0.0]` sections
- `1.1.0 Added` subsection lists all 14+ features from Phases 6–20
- `1.1.0 Fixed` subsection lists the exact 6 bug fixes from Wave 1
- Date for 1.1.0 is `2026-05-12`
  </acceptance_criteria>
  </task>

<task>
<id>21-04-c</id>
<title>Create CONTRIBUTING.md with developer guidelines and PR template</title>
<read_first>
  - GETTING_STARTED.md
  - .planning/codebase/CONVENTIONS.md (if exists)
  - .planning/phases/21-final-polish-marketplace-release/21-RESEARCH.md (section 5.3)
</read_first>
<action>
Create CONTRIBUTING.md in the project root with the following sections:

```markdown
# Contributing to OSV IntelliJ Plugin

Thank you for your interest! This guide covers setup, code style, and the PR process.

## Development Setup

1. JDK 17+ (Temurin recommended)
2. IntelliJ IDEA 2023.3+ (Community or Ultimate)
3. Clone the repo: `git clone https://github.com/dyuti/jetbrains-osv-plugin.git`
4. Import as Gradle project
5. Run: `./gradlew runIde` (starts a sandbox IntelliJ with the plugin loaded)

## Code Style

- Kotlin with 4-space indentation
- Max line length: 120 characters
- Use `JBColor` for all UI colors (never raw `java.awt.Color`)
- All long-running operations go in `ProgressManager.run(Backgroundable(...))`
- All document mutations use `WriteCommandAction.runWriteCommandAction()`
- All logging uses `com.intellij.openapi.diagnostic.logger<T>()` (never `System.err`)

## Testing

- All new features must include unit tests
- Use MockWebServer for HTTP-dependent tests (no live API calls in CI)
- Use `BasePlatformTestCase` / `HeavyPlatformTestCase` for PSI inspection tests
- Run full suite: `./gradlew test`
- Minimum target: maintain or improve current test count (282+)

## Pull Request Process

1. Branch from `main`: `git checkout -b feature/short-description`
2. Include tests for all new code
3. Ensure `./gradlew test` passes locally
4. Update CHANGELOG.md under `[Unreleased]`
5. Open PR with a clear description of what changed and why

## Reporting Bugs

- Use GitHub Issues
- Include: IDE version, plugin version, OS, steps to reproduce, stack trace if applicable
```

</action>
<acceptance_criteria>
  - CONTRIBUTING.md exists in project root
  - CONTRIBUTING.md contains sections for Development Setup, Code Style, Testing, Pull Request Process, and Reporting Bugs
  - Code Style section explicitly mentions `JBColor`, `Backgroundable`, `WriteCommandAction`, and `logger<T>()`
  - Minimum test target (282+) is referenced in Testing section
</acceptance_criteria>
</task>

<task>
<id>21-04-d</id>
<title>Create FAQ.md with proxy, rate limit, false positive, and troubleshooting guidance</title>
<read_first>
  - .planning/phases/21-final-polish-marketplace-release/21-RESEARCH.md (section 5.4)
  - src/main/java/io/dyuti/osvplugin/config/OsVConfig.kt (config options)
</read_first>
<action>
Create FAQ.md in the project root with the following content:

```markdown
# OSV Plugin FAQ & Troubleshooting

## General

### Q: The plugin says "No vulnerabilities found" but I know my dependency has a CVE. Why?

- The OSV database may not index that specific CVE yet (coverage is excellent but not exhaustive).
- Check that your dependency file is one of the supported formats: `pom.xml`, `build.gradle`, `build.gradle.kts`, `package-lock.json`, `requirements.txt`.
- Verify the dependency is not in your Ignored Packages list (Settings → OSV Scanner).
- Try clearing the cache: the plugin caches results for 1 hour by default.

### Q: How do I suppress a false positive?

- Use the Alt+Enter quick-fix on the highlighted dependency and select "Suppress for this dependency".
- Or add the package name to Settings → OSV Scanner → Ignored Packages.
- Adjust the Minimum Severity threshold if the finding is LOW and you want to hide it.

## Proxy & Network

### Q: My company uses an HTTP proxy. How do I configure the plugin?

The plugin uses `java.net.http.HttpClient`, which respects standard JVM system properties:
```

-Dhttp.proxyHost=proxy.company.com
-Dhttp.proxyPort=8080
-Dhttps.proxyHost=proxy.company.com
-Dhttps.proxyPort=8080

```
Set these in Help → Edit Custom VM Options... and restart IntelliJ IDEA.

### Q: Can I use a self-hosted OSV API?
Yes. Go to Settings → Tools → OSV Scanner and change the "OSV API URL" field.

## Rate Limiting

### Q: Is there a rate limit?
The OSV public API does not enforce strict rate limits for open queries.
If you enable GitHub Advisory integration, the unauthenticated rate limit is 60 requests/hour.
Add a GitHub token in Settings → OSV Scanner to raise this to 5000 requests/hour.

## Inspections & UI

### Q: Why aren't inline highlights showing in my dependency file?
1. Ensure the inspection is enabled: Settings → Editor → Inspections → Security → "OSV Vulnerability Check"
2. Check your Minimum Severity threshold — if set to MEDIUM, LOW findings are hidden
3. Verify the file type is supported (pom.xml, build.gradle, etc.)
4. The inspection runs asynchronously; wait a few seconds after opening the file

### Q: What is "Focus Mode"?
Focus Mode (Branch Comparison) shows only vulnerabilities that are NEW in your current branch compared to a configurable base branch (default: `main`).

## Known Limitations

- Gradle version catalogs (`libs.versions.toml`) are not yet supported for dependency parsing.
- Historical trends use ASCII sparklines; a proper chart library is planned for a future release.
- Line-level problem markers (exact-line navigation in the Problems tool window) are a planned enhancement.
```

</action>
<acceptance_criteria>
  - FAQ.md exists in project root
  - FAQ.md contains at least 6 Q&A pairs covering proxy, rate limiting, false positives, troubleshooting, and known limitations
  - Known Limitations section lists: Gradle version catalogs unsupported, ASCII sparklines, line-level problem markers deferred
</acceptance_criteria>
</task>

<task>
<id>21-04-e</id>
<title>Document known Gradle parser limitations in GradleParser.kt and FAQ</title>
<read_first>
  - src/main/java/io/dyuti/osvplugin/parser/GradleParser.kt
  - FAQ.md (created in task 21-04-d)
</read_first>
<action>
In GradleParser.kt, add KDoc comments at the top of the file documenting supported and unsupported syntax:

```kotlin
/**
 * Parses Gradle dependency declarations from build scripts.
 *
 * **Supported syntax:**
 * - `implementation("group:artifact:version")`
 * - `testImplementation("group:artifact:version")`
 * - `api("group:artifact:version")`
 * - `compileOnly("group:artifact:version")`
 * - `runtimeOnly("group:artifact:version")`
 *
 * **Not supported (known limitations):**
 * - `platform()` / `enforcedPlatform()` wrapper dependencies
 * - Gradle version catalogs (`libs.versions.toml`)
 * - Kotlin DSL `implementation(group = "...", name = "...", version = "...")` map syntax
 * - String interpolation / variable references for version numbers
 *
 * These patterns are parsed as best-effort; fallback to manual review if parsing appears incomplete.
 */
```

Also verify the regex in `GradleParser.kt` already handles `api`, `testImplementation`, `compileOnly`, `runtimeOnly`. If not, extend the regex pattern to match these configuration names. The existing regex likely matches `implementation` only — extend it to:

```
(val |\b)(implementation|api|testImplementation|compileOnly|runtimeOnly)\s*\(["']([^"']+)["']\)
```

In FAQ.md, confirm the "Known Limitations" section already mentions `libs.versions.toml` (added in 21-04-d).
</action>
<acceptance_criteria>

- GradleParser.kt has a KDoc block listing at least 4 supported and 4 not-supported syntax patterns
- GradleParser.kt regex handles `implementation`, `api`, `testImplementation`, `compileOnly`, `runtimeOnly`
- FAQ.md "Known Limitations" section mentions Gradle version catalogs
  </acceptance_criteria>
  </task>

<task>
<id>21-04-f</id>
<title>Update GETTING_STARTED.md and PLUGIN_DOCUMENTATION.md for v1.1.0 features</title>
<read_first>
  - GETTING_STARTED.md
  - PLUGIN_DOCUMENTATION.md
  - .planning/ROADMAP.md (phases 6–20 features)
</read_first>
<action>
In GETTING_STARTED.md:
1. Add a "What's New in v1.1.0" section after Installation summarizing the 3 most impactful new features: Risk Scoring, Policy Enforcement, and SBOM Generation.
2. Update the "Understanding the Tool Window" section to mention the 3 tabs: Vulnerabilities, Trends, SBOM.
3. Update "Configuration" section to mention the new OSV API URL field.
4. Add a "Privacy Mode" subsection explaining how to enable privacy-preserving hashes.

In PLUGIN_DOCUMENTATION.md:

1. Expand the feature matrix/table to include all v1.1.0 features with one-sentence descriptions.
2. Add a "Security Features" section covering: Malicious Package Detection, SAST, Config Audit, Risk Scoring, Policy Enforcement.
3. Add a "Team Features" section covering: Team Config, Differential Analysis, Historical Trends, Notifications.
4. Ensure the "Known Limitations" subsection is consistent with FAQ.md.
   </action>
   <acceptance_criteria>

- GETTING_STARTED.md contains a "What's New in v1.1.0" section
- GETTING_STARTED.md mentions 3 tabs in the tool window (Vulnerabilities, Trends, SBOM)
- PLUGIN_DOCUMENTATION.md has a feature matrix covering all Phases 6–20 features
- PLUGIN_DOCUMENTATION.md has "Security Features" and "Team Features" sections
- Both files are free of references to unimplemented features as fully functional
  </acceptance_criteria>
  </task>

## Verification Criteria

- README.md, CHANGELOG.md, CONTRIBUTING.md, FAQ.md all exist in project root
- README.md contains competitive comparison table with ≥15 rows
- CHANGELOG.md documents v1.0.0 and v1.1.0 with Keep a Changelog format
- CONTRIBUTING.md has code style, testing, and PR process sections
- FAQ.md covers proxy, rate limit, false positives, troubleshooting, known limitations
- GETTING_STARTED.md and PLUGIN_DOCUMENTATION.md updated for v1.1.0

## must_haves

- README.md is a compelling first impression for marketplace visitors
- CHANGELOG.md accurately records every Phase 6–20 feature
- CONTRIBUTING.md sets clear expectations for new contributors
- FAQ.md reduces support burden by answering expected questions
- GradleParser.kt documents its own limitations inline (KDoc)
